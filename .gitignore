node_modules/
data/
migration/review/shots*/
migration/review/*-proof.png

# pipeline 監控 DB 化（2026-09-02）：repo-local .env，跟 telegram-dispatcher
# 同一套紀律——只擋 dotenv 格式，.env.example 是無值範本，要能進 git。
.env
.env.*
!.env.example

# worktree 隔離型 subagent 派工產生的暫存 worktree（2026-09-04），本機產物不進 git。
.claude/worktrees/
