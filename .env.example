# tg-monitor 環境變數範本——只有 KEY，沒有值，可進 git。
# 用法：cp .env.example .env，填入真實值後 chmod 600（那份 .env 不可進 git）。
#
# pipeline 監控 DB 化（2026-09-02）。見
# telegram-dispatcher/deploy/monitor-db/README.md 與
# plan-db-as-truth-v3.md §8.0 / §4.2（tg-monitor 用 mon_ui 帳號，
# 唯讀 + runs 欄位級 cancel 寫入；不給任何欄位加密金鑰——那些只放 head 的
# telegram-dispatcher/.env）。

# 開關：未設定／空字串／'0' 一律視同關閉，不影響任何現有行為。
MON_DB_ENABLED=
# 讀取面資料源（Phase 8，plan §8.1）：sqlite|mysql，未設／空字串／sqlite 皆為
# sqlite（行為與遷移前完全相同）。與 MON_DB_ENABLED **各自獨立**：讀取面可以在
# 寫入面還關著時先切過去對已有資料做 A/B 對照。回滾＝把這個值改回 sqlite 後
# `launchctl kickstart -k com.aladdin.tg-monitor`，不需要 commit / push。
MON_READ_SOURCE=
# 監控 DB（mon-mysql container，127.0.0.1:3307，經 head 的 tunnel 或 head 本機 loopback）
MON_DB_HOST=
MON_DB_PORT=
MON_DB_SCHEMA=
# tg-monitor 專用帳號（唯讀九張表 + runs 欄位級 INSERT/UPDATE，見 §2.2）
MON_DB_USER=
MON_DB_PASSWORD=
